# sdt-greet 空姓名输入问题修复记录

## Issue

### 标题

修复 sdt-greet 对空白姓名参数校验不足的问题

### 环境

操作系统：Windows

运行环境：Python 3.9

### 问题描述

sdt-greet 命令行工具目前未对 name 参数进行有效性检查。

当用户输入只包含空格的姓名时：

sdt-greet --name "   "

程序仍然输出：

Hello, !

并返回退出状态码 0。

该行为导致无效参数被接受，不符合预期。

### 复现步骤

1. 安装并运行 sdt-greet。
2. 执行命令：

sdt-greet --name "   "

3. 查看终端输出和退出状态。

### 期望结果

当 name 参数为空或仅包含空格时，程序应提示输入错误，并返回非正常退出状态。

### 实际结果

程序正常执行，并输出无效的问候信息。

### 未知信息

导致该问题的具体原因待确认。


## 提交信息

### 修复空姓名参数校验问题

在命令行参数解析后增加 name 参数检查。

当检测到输入内容为空或仅包含空白字符时，程序主动退出并返回错误状态，避免生成无效输出。

同时保留正常姓名输入时的原有功能。


## 评审意见

### Review Result

通过。

### Comments

Blocking：

无阻塞问题，可以合并。

Suggestion：

建议增加空字符串和全空格字符串的自动化测试，防止后续修改导致该问题重新出现。

Nit：

错误提示信息可以进一步优化，例如明确提示用户重新输入有效姓名。

本次修改解决了空姓名输入问题，修改范围合理，没有影响其他正常功能。